import React from 'react'
import type { Metadata } from 'next';
import LibraryTopbar from '@/src/components/library/navigation/topbar';
import LibrarySidebar from '@/components/library/navigation/sidebar';
import { cn } from '@/src/lib/utils';

export const metadata: Metadata = {
    title: 'Traveller Library',
    description: 'Generated by create next app',
}

export default async function LibraryLayout ({
    children
}: {
    children: React.ReactNode
}) {
    const content = (
        <>
            <div className='fixed z-20 top-0 w-full'>
                <LibraryTopbar className='' />
            </div>
            <div className={cn(
                    `relative mx-auto`, 
                    `pt-16 lg:pt-12`,
                    `lg:grid lg:grid-cols-[250px_1fr_]`,
                    `max-w-screen-xl min-w-[500px]`
                )}>
                <aside id="Library-Sidebar" className="hidden lg:block relative">
                    <div className='fixed z-20 top-0 mt-20 overflow-hidden overflow-y-auto w-[250px]'>
                        <LibrarySidebar className="px-3" />
                    </div> 
                </aside>
            
                <main className='w-full'>
                { children}
                </main>

            </div>
        </>
    )
    return content
}
